version: "3.8"

services:
  app:
    image: novan-webapp:latest
    build:
      context: .
      dockerfile: Dockerfile
      target: runner
    env_file:
      - .env.production
    environment:
      NODE_ENV: production
      PORT: 3000
    ports:
      - "3000:3000"
    restart: unless-stopped

