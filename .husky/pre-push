#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

node scripts/check-unused-ts-files.js
RESULT=$?
if [ $RESULT -ne 0 ]; then
  echo "\n❌ Push blocked: Unused .ts/.tsx files detected. Please remove or use them before pushing."
  exit 1
fi 